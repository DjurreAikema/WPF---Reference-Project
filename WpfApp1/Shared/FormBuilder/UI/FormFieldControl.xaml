<abstract:BDumbComponent x:Class="WpfApp1.Shared.FormBuilder.UI.FormFieldControl"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                         xmlns:abstract="clr-namespace:WpfApp1.Shared.Abstract"
                         xmlns:ui1="clr-namespace:WpfApp1.Shared.FormBuilder.UI"
                         mc:Ignorable="d">

    <Grid DataContext="{Binding RelativeSource={RelativeSource AncestorType=ui1:FormFieldControl}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0"
                 Text="{Binding FieldValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 BorderBrush="{Binding ValidationBrush}"/>

        <TextBlock Grid.Row="1"
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   Visibility="{Binding ErrorVisibility}"
                   TextWrapping="Wrap"
                   Margin="0,2,0,0"
                   FontSize="11"/>
    </Grid>
</abstract:BDumbComponent>
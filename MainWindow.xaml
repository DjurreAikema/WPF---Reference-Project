<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.Column="0" Content="Open Window One"
                Padding="10" Margin="5"
                Click="OpenWindowOneButton_Click"/>

        <Button Grid.Row="1" Grid.Column="0" Content="Open Window Two"
                Padding="10" Margin="5"
                Click="OpenWindowTwoButton_Click"/>

        <Button Grid.Row="2" Grid.Column="0" Content="Open Window Three"
                Padding="10" Margin="5"
                Click="OpenWindowThreeButton_Click"/>

    </Grid>
</Window>
